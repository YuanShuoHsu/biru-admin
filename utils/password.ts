export const handleMouseDownPassword = (
  event: React.MouseEvent<HTMLButtonElement>,
) => event.preventDefault();

export const handleMouseUpPassword = (
  event: React.MouseEvent<HTMLButtonElement>,
) => event.preventDefault();
